class Planner {
  constructor(data) {
    this.data = data;
  }

  save() {
    // Logic to save planner data
  }

  find() {
    // Logic to find planner data
  }

  update(id, newData) {
    // Logic to update planner data
  }

  delete(id) {
    // Logic to delete planner data
  }
}

module.exports = Planner;
